// Placeholder for Task 13 (admin Claims tab). The (admin) layout already gates this route on
// requireAdmin() and renders the Claims/Employees nav — this page just fills the tab body.
export default function AdminClaimsPage() {
  return <p className="text-sm text-zinc-600 dark:text-zinc-400">Claims view coming next.</p>
}
